{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ],
  "buildCommand": "pip install --no-cache-dir -q -r backend/requirements.txt; cd frontend && npm run build",
  "outputDirectory": "frontend/build"
}

