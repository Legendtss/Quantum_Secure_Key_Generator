{
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/build",
  "installCommand": "npm install --legacy-peer-deps && cd backend && pip install -r requirements.txt",
  "env": {
    "PYTHON_VERSION": "3.9"
  },
  "functions": {
    "api/index.py": {
      "runtime": "python3.9"
    }
  }
}
