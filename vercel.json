{
  "buildCommand": "pip install --break-system-packages -q -r backend/requirements.txt 2>&1 && cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/build",
  "functions": {
    "api/*.py": {
      "runtime": "python3.11"
    }
  }
}

